name: SINAC MAPFRE pilot

# This workflow is intentionally inert after the one-time pilot run.
# No Dropbox URL, row-level data, or credentials are stored in the current branch.
# The completed 2023 pilot used a short-lived file-specific URL to a trusted
# Dropbox copy and printed aggregate results only.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SINAC MAPFRE pilot relay sanitized; no data transfer configured."
