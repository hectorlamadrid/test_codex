name: SINAC MAPFRE pilot

# Inert placeholder after completed one-time facility pilot.
# No Dropbox URL, row-level data, or credentials are stored in the current branch.
# Aggregate pilot results only were retained in GitHub Actions logs.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SINAC MAPFRE pilot relay sanitized; no data transfer configured."
